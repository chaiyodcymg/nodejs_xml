<!DOCTYPE html>
<html lang="en">
<%- include('head') %>
<body>
    <% if(role == null){ %>
        <%- include('header_notlogin') %>
      <% } else if(role == 1) {%>
        <%- include('header_admin') %>
      <%}else{ %>
        <%- include('header_user') %>
    <% } %>
    <%- include('banner') %>

    <div class="container catinfo">
        <div class="row">
          <div class="col">
            <div class="img_presentcat">
              <img src="<%= cat.pet_image%>" alt="">
            </div>
          </div>
          <div class="col-xl catinfo_text">
            <div>
              <div class="text-wrap">
                <h3><%= cat.pet_name%></h3>
                <ul>
                  <li>เรื่องที่แจ้ง: <%= cat.post_type%></li>
                  <li>เพศ: <%= cat.pet_gender%></li>
                  <li>สี: <%= cat.pet_color%></li>
                  <li>วัคซีนที่เคยได้รับ: <%= cat.pet_vaccin%></li>
                  <li>สายพันธ์: <%= cat.pet_gene%></li>
                  <li>วัคซีน: <%= cat.pet_vaccine%></li>
                  <li>วันที่ได้รับวัคซีน: <%= cat.pet_vaccin_date%></li>
                  <li>สถานที่พบ/หาย/ที่อยู่:<%= cat.place_landmarks%> <%= cat.place_name%> <%= cat.place%> </li>
                </ul>
              </div>
              <div class="text-wrap">
                <h3>ข้อมูลสัตว์</h3>
                <p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<%= cat.pet_symptom%></p>
              </div>
              <div class="text-wrap">
                <h3>ช่องทางการติดต่อ</h3>
                <ul>
                  <li> ชื่อ: <%= cat.contact_name %> <%= cat.contact_surname %></li>
                  <li> เบอร์โทร: <%= cat.contact_tel %></li>
                  <li> อีเมล: <%= cat.contact_email %></li>
                  <li> LineID: <%= cat.contact_line %></li>
                  <li> Facebook: <%= cat.contact_facebook %></li>
                  <br>
                  <li class="fa-solid fa-phone"> เบอรโทร: <%= cat.contact_tel %></li>
                </ul>    
                <div>วันที่โพสต์ <%= cat.createat %></div>         
              </div>
            </div>        
          </div>
        </div>
      </div>

    <%- include('./footer'); %>
</body>
</html>