<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <!-- for each พวกในตาราง -->
    <!-- <% docs.forEach(docs => { %>
        <h1><%= docs.name %></h1>
        <img src="<%= docs.image %>" style="width: 150px;height:150px;">
        <p><%= docs.color %></p>
        <p><%= docs.species %></p>
    <% }) %> -->

    <% Object.values(docs).forEach(function(item){%>
        <a href="<%= item._id %>"><h1><%= item.name %></h1></a>
        <img src="<%= item.image %>" style="width: 150px;height:150px;">
        <p><%= item.color %></p>
        <p><%= item.species %></p>
    <% }) %>
   
</body>
</html>