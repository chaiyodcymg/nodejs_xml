<%- include('head') %>

<body>
    <% if(role == null){ %>
        <%- include('header_notlogin') %>
      <% } else if(role == 1) {%>
        <%- include('header_admin') %>
      <%}else{ %>
        <%- include('header_user') %>
        <%}%>
        <div class="container">
            <div class="box-wrap-profile">
                <div class="profile-info">
                    <div class="row">
                        <div class="col-6">
                            <p class="fs-5 text-end">ชื่อ : </p>
                        </div>
                        <div class="col-6"><%= profile.firstname %>  <%= profile.lastname %></div>
                    </div>
                    <div class="row">
                        <div class="col-6">
                            <p class="fs-5 text-end">อีเมล : </p>
                        </div>
                        <div class="col-6"><%= profile.email %></div>
                    </div>
                    <div class="row">
                        <div class="col-6">
                            <p class="fs-5 text-end">เบอร์โทร : </p>
                        </div>
                        <div class="col-6">xxxxx</div>
                    </div>
                </div>
                <a href="editprofile"><button class="editprofile">แก้ไขโปรไฟล์</button></a>
            
                <a href="/logout"><button class="btn btn-danger logout" type="button">ออกจากระบบ</button></a>
            </div>
        </div>
    <%- include('footer') %>
